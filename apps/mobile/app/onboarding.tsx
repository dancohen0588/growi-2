import { OnboardingCarousel } from '@/components/onboarding/OnboardingCarousel'

/** Présentation du premier lancement — route racine, hors pile `(auth)`. */
export default function OnboardingScreen() {
  return <OnboardingCarousel />
}
