// Schémas du formulaire Paramètres — définis dans @growi/shared pour être
// partagés avec l'app mobile. Ce module reste le point d'entrée du web.
export { profilSchema, changePasswordSchema } from '@growi/shared'
export type { ProfilInput, ChangePasswordInput } from '@growi/shared'
