// Schémas d'authentification — définis dans @growi/shared pour être partagés
// avec l'app mobile. Ce module reste le point d'entrée historique du web.
export { loginSchema, registerSchema } from '@growi/shared'
export type { LoginInput, RegisterInput } from '@growi/shared'
