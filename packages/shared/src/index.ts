/**
 * @growi/shared — types, schémas Zod et constantes métier partagés
 * entre l'app web (apps/web) et l'app mobile (apps/mobile).
 *
 * Le contenu réel (schémas du domaine Growi : User, Garden, PlantInstance,
 * logs d'entretien, DTOs) est ajouté à l'étape 1.2 du plan mobile.
 */

export {}
